import {makeStyles} from "@mui/styles";

export const useStyles = makeStyles(({
    noItemsPrompt: {
        position: "absolute",
        top: "50%",
         left: "50%",
         transform: "translate(-50%, -50%)",
    }
}))