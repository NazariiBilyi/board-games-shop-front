import {makeStyles} from "@mui/styles";

export const useStyles = makeStyles({
    root: {
        width: '100%',
        height: '100%',
        position: "relative"
    },
    dropdown: {
        maxWidth: '300px'
    },
    tableWrapper: {
        padding: "20px 20px 20px 0"
    }
})